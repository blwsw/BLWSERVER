<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hopedove.gatewayserver.dao.IGatewayDao">

    <select id="getGatewayApplication" parameterType="GatewayApplication" resultType="GatewayApplication">
        select * from gateway_application
        where appId = #{appId}
    </select>

</mapper>